const express= require('express')
const app = express()

const rendering = {
    
    app.use('../frontend/css', express.static(__dirname + "frontend/css"))
    app.use('../frontend/css', express.static(__dirname + "frontend/css"))
    app.use('../frontend/css', express.static(__dirname + "frontend/css"))
}